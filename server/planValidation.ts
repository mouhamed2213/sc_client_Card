// The plan rules live in `shared/` so the studio can preview them (which
// fiche can be activated right away, what is missing) with the exact code the
// server enforces. Kept here as a re-export for existing server imports.
export * from "@shared/planValidation";
