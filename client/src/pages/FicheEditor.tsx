import { Button } from "@/components/ui/button";
import AdminClientInvitationPanel from "@/components/AdminClientInvitationPanel";
import { prepareImage } from "@/lib/imageProcessing";
import { trpc } from "@/lib/trpc";
import type { MediaKind } from "@shared/mediaRules";
import { getPlanFeatures, type PlanName } from "@shared/planFeatures";
import { ArrowLeft, Check, Eye, ImagePlus, Loader2, Plus, Save, Trash2, Upload } from "lucide-react";
import { useEffect, useMemo, useState } from "react";
import { toast } from "sonner";
import { Link, useLocation, useParams } from "wouter";

const planLabels: Record<PlanName, string> = { essentiel: "Essentiel", pro: "Pro", signature: "Signature" };
const weekdays = ["Lundi", "Mardi", "Mercredi", "Jeudi", "Vendredi", "Samedi", "Dimanche"];
type Status = "active" | "suspendue" | "supprimee" | "brouillon";
type LinkItem = { label: string; url: string };
type SocialItem = { label: string; url: string };
type GalleryItem = { url: string; alt: string };
type HoursItem = { jour: string; horaire: string };
type Article = { nom: string; description: string; prix: string };
type CatalogSection = { titre: string; articles: Article[] };
type EditorForm = {
  id: number; slug: string; formule: PlanName; statut: Status; nom: string; prenom: string; fonction: string; entreprise: string;
  telephone: string; whatsapp: string; email: string; site: string; adresse: string; lienItineraire: string; googlePlaceId: string; photo: string; logo: string;
  data: { premierBouton: "whatsapp" | "appel" | "contact"; messageWhatsapp: string; presentation: string; rendezVous: LinkItem; reseauxSociaux: SocialItem[]; liens: LinkItem[]; horaires: HoursItem[]; galerie: GalleryItem[]; sections: CatalogSection[]; notesInternes: string; };
};
function fileToDataUrl(file: File) { return new Promise<string>((resolve, reject) => { const reader = new FileReader(); reader.onload = () => resolve(String(reader.result)); reader.onerror = reject; reader.readAsDataURL(file); }); }
export default function FicheEditor() {
  const { slug = "" } = useParams<{ slug: string }>();
  const [, navigate] = useLocation();
  const ficheQuery = trpc.fiches.getBySlug.useQuery({ slug });
  const requestsQuery = trpc.fiches.contactRequests.useQuery({ slug });
  const updateMutation = trpc.fiches.update.useMutation();
  const uploadMutation = trpc.media.upload.useMutation();
  const [form, setForm] = useState<EditorForm | null>(null);
  const [uploading, setUploading] = useState<MediaKind | null>(null);
  useEffect(() => { if (!ficheQuery.data || form) return; const fiche = ficheQuery.data; setForm({ id: fiche.id, slug: fiche.slug, formule: fiche.formule, statut: fiche.statut, nom: fiche.nom, prenom: fiche.prenom, fonction: fiche.fonction, entreprise: fiche.entreprise, telephone: fiche.telephone, whatsapp: fiche.whatsapp, email: fiche.email ?? "", site: fiche.site ?? "", adresse: fiche.adresse ?? "", lienItineraire: fiche.lienItineraire ?? "", googlePlaceId: fiche.googlePlaceId ?? "", photo: fiche.photo ?? "", logo: fiche.logo ?? "", data: { premierBouton: fiche.data.premierBouton ?? "whatsapp", messageWhatsapp: fiche.data.messageWhatsapp ?? "", presentation: fiche.data.presentation ?? "", rendezVous: fiche.data.rendezVous ?? { label: "Prendre rendez-vous", url: "" }, reseauxSociaux: fiche.data.reseauxSociaux ?? [], liens: fiche.data.liens ?? [], horaires: fiche.data.horaires ?? [], galerie: fiche.data.galerie ?? [], sections: fiche.data.sections ?? [], notesInternes: fiche.data.notesInternes ?? "" } }); }, [ficheQuery.data, form]);
  const features = useMemo(() => (form ? getPlanFeatures(form.formule) : null), [form?.formule]);
  if (ficheQuery.isLoading || !form || !features) return <div className="public-loading"><Loader2 className="h-6 w-6 animate-spin" /><p>Chargement de l’éditeur…</p></div>;
  if (ficheQuery.error) return <div className="p-10 text-center">Fiche introuvable.</div>;
  const setField = <K extends keyof EditorForm>(key: K, value: EditorForm[K]) => setForm(current => (current ? { ...current, [key]: value } : current));
  const setData = <K extends keyof EditorForm["data"]>(key: K, value: EditorForm["data"][K]) => setForm(current => current ? { ...current, data: { ...current.data, [key]: value } } : current);
  function selectPlan(plan: PlanName) { const next = getPlanFeatures(plan); setForm(current => { if (!current) return current; const horaires = next.requiresHours && current.data.horaires.length !== 7 ? weekdays.map(jour => ({ jour, horaire: "" })) : current.data.horaires; return { ...current, formule: plan, data: { ...current.data, horaires } }; }); }
  async function upload(file: File, kind: MediaKind) { if (!form) return; try { setUploading(kind); const prepared = await prepareImage(file, kind); const result = await uploadMutation.mutateAsync({ formula: form.formule, kind, filename: prepared.name, mimeType: "image/webp", contentBase64: await fileToDataUrl(prepared) }); if (kind === "profile") setField("photo", result.url); if (kind === "logo") setField("logo", result.url); if (kind === "gallery") setData("galerie", [...form.data.galerie, { url: result.url, alt: prepared.name }]); toast.success("Image préparée et enregistrée", { description: `${Math.round(result.bytes / 1024)} ko` }); } catch (error) { toast.error("Image refusée", { description: error instanceof Error ? error.message : "Le traitement a échoué." }); } finally { setUploading(null); } }
  async function save(status: Status) { if (!form) return; try { const result = await updateMutation.mutateAsync({ ...form, statut: status }); setField("statut", status); toast.success(status === "active" ? "Fiche validée et active" : "Brouillon enregistré"); if (result.slug !== slug) navigate(`/studio/fiche/${result.slug}`); } catch (error) { toast.error("La fiche n’est pas conforme au plan", { description: error instanceof Error ? error.message : "Vérifiez les champs obligatoires." }); } }
  return <div className="min-h-screen bg-[#f5f6f8] text-[#172033]"><header className="sticky top-0 z-20 border-b border-[#e3e6ea] bg-white/95 backdrop-blur"><div className="mx-auto flex max-w-[1300px] items-center justify-between gap-4 px-5 py-4"><div className="flex items-center gap-3"><Link href="/" className="icon-button" title="Retour"><ArrowLeft className="h-4 w-4" /></Link><div><p className="eyebrow">Éditeur de fiche</p><h1 className="font-semibold">{form.prenom} {form.nom}</h1></div></div><div className="flex gap-2"><Button><Eye className="h-4 w-4" /> Voir la fiche</Button><Button variant="outline" onClick={() => save("brouillon")} disabled={updateMutation.isPending}><Save className="h-4 w-4" /> Enregistrer</Button><Button onClick={() => save("active")} disabled={updateMutation.isPending} className="bg-[#172033] text-white"><Check className="h-4 w-4" /> Valider et activer</Button></div></div></header><main className="mx-auto grid max-w-[1300px] gap-6 px-5 py-7 lg:grid-cols-[290px_1fr]"><aside className="space-y-5"><section className="editor-card sticky top-24"><p className="eyebrow">Formule</p><div className="mt-3 grid gap-2">{(Object.keys(planLabels) as PlanName[]).map(plan => <button type="button" key={plan} onClick={() => selectPlan(plan)} className={`plan-choice ${form.formule === plan ? "plan-choice-active" : ""}`}><span>{planLabels[plan]}</span>{form.formule === plan && <Check className="h-4 w-4" />}</button>)}</div><div className="mt-5 rounded-xl bg-[#f5f7fa] p-4 text-xs leading-6 text-[#667085]"><p><strong>{features.maxLinks}</strong> liens personnalisés</p><p><strong>{features.maxPhotos}</strong> photos de galerie</p><p>Portrait/logo {features.requiresProfile ? "obligatoire" : "optionnel"}</p><p>Formulaire {features.hasForm ? "inclus" : "non inclus"}</p><p>Avis Google {features.hasGoogleReview ? "obligatoire" : "non inclus"}</p><p>Catalogue {features.hasCatalog ? "inclus" : "non inclus"}</p></div>{form.data.liens.length > features.maxLinks && <p className="mt-3 text-xs font-medium text-red-600">Supprimez {form.data.liens.length - features.maxLinks} lien(s) avant validation.</p>}{form.data.galerie.length > features.maxPhotos && <p className="mt-2 text-xs font-medium text-red-600">Supprimez {form.data.galerie.length - features.maxPhotos} photo(s) avant validation.</p>}</section></aside><div className="space-y-6"><EditorSection title="Identité" note="Visible dans la première zone, sans défilement"><div className="editor-grid"><EditorField label="Prénom"><input value={form.prenom} onChange={e => setField("prenom", e.target.value)} /></EditorField><EditorField label="Nom"><input value={form.nom} onChange={e => setField("nom", e.target.value)} /></EditorField><EditorField label="Fonction"><input value={form.fonction} onChange={e => setField("fonction", e.target.value)} /></EditorField><EditorField label="Entreprise"><input value={form.entreprise} onChange={e => setField("entreprise", e.target.value)} /></EditorField><EditorField label="Slug"><input value={form.slug} onChange={e => setField("slug", e.target.value.toLowerCase().replace(/[^a-z-]/g, "-"))} /></EditorField><EditorField label="Action prioritaire"><select value={form.data.premierBouton} onChange={e => setData("premierBouton", e.target.value as EditorForm["data"]["premierBouton"])}><option value="whatsapp">WhatsApp</option><option value="appel">Appeler</option><option value="contact">Enregistrer le contact</option></select></EditorField></div></EditorSection><EditorSection title="Portrait et logo" note="Portrait recadré automatiquement à 400 × 400 sous 30 ko; logo sous 80 ko"><div className="media-pair"><MediaPicker title="Portrait" value={form.photo} required={features.requiresProfile && !form.logo} loading={uploading === "profile"} onPick={file => upload(file, "profile")} onRemove={() => setField("photo", "")} /><MediaPicker title="Logo" value={form.logo} required={features.requiresProfile && !form.photo} loading={uploading === "logo"} onPick={file => upload(file, "logo")} onRemove={() => setField("logo", "")} /></div></EditorSection><EditorSection title="Contact et localisation"><div className="editor-grid"><EditorField label="Téléphone"><input value={form.telephone} onChange={e => setField("telephone", e.target.value)} /></EditorField><EditorField label="WhatsApp"><input value={form.whatsapp} onChange={e => setField("whatsapp", e.target.value)} /></EditorField><EditorField label="E-mail"><input type="email" value={form.email} onChange={e => setField("email", e.target.value)} /></EditorField><EditorField label="Site"><input value={form.site} onChange={e => setField("site", e.target.value)} /></EditorField><EditorField label="Adresse"><input value={form.adresse} onChange={e => setField("adresse", e.target.value)} /></EditorField><EditorField label="Lien itinéraire"><input value={form.lienItineraire} onChange={e => setField("lienItineraire", e.target.value)} /></EditorField></div><EditorField label="Message WhatsApp prérempli"><textarea value={form.data.messageWhatsapp} onChange={e => setData("messageWhatsapp", e.target.value)} /></EditorField></EditorSection>{form.formule !== "essentiel" && <><EditorSection title="Présentation Pro" note="Bloc affiché sur les formules Pro et Signature"><EditorField label="Présentation de l’activité"><textarea rows={6} placeholder="Présentez l’activité, les services, l’expertise ou le positionnement de l’établissement…" value={form.data.presentation} onChange={e => setData("presentation", e.target.value)} /></EditorField></EditorSection><EditorSection title="Rendez-vous" note="Un bouton direct vers la page de réservation"><div className="editor-grid"><EditorField label="Libellé du bouton"><input value={form.data.rendezVous.label} onChange={e => setData("rendezVous", { ...form.data.rendezVous, label: e.target.value })} placeholder="Prendre rendez-vous" /></EditorField><EditorField label="URL de réservation"><input type="url" value={form.data.rendezVous.url} onChange={e => setData("rendezVous", { ...form.data.rendezVous, url: e.target.value })} placeholder="https://…" /></EditorField></div></EditorSection><EditorSection title={`Réseaux sociaux (${form.data.reseauxSociaux.length})`} note="Ils seront affichés comme des boutons avec leur icône, adaptés au thème de la fiche"><div className="space-y-3">{form.data.reseauxSociaux.map((social, index) => <div className="repeater-row" key={`${social.url}-${index}`}><input placeholder="Instagram, Facebook, LinkedIn…" value={social.label} onChange={e => setData("reseauxSociaux", form.data.reseauxSociaux.map((item, i) => i === index ? { ...item, label: e.target.value } : item))} /><input type="url" placeholder="https://…" value={social.url} onChange={e => setData("reseauxSociaux", form.data.reseauxSociaux.map((item, i) => i === index ? { ...item, url: e.target.value } : item))} /><button type="button" className="icon-button" onClick={() => setData("reseauxSociaux", form.data.reseauxSociaux.filter((_, i) => i !== index))}><Trash2 className="h-4 w-4" /></button></div>)}<Button type="button" variant="outline" onClick={() => setData("reseauxSociaux", [...form.data.reseauxSociaux, { label: "", url: "" }])}><Plus className="h-4 w-4" /> Ajouter un réseau</Button></div></EditorSection></>}{features.hasLinks && <EditorSection title={`Liens (${form.data.liens.length}/${features.maxLinks})`} note="Liens d’action supplémentaires"><div className="space-y-3">{form.data.liens.map((item, index) => <div className="repeater-row" key={`${item.url}-${index}`}><input placeholder="Libellé" value={item.label} onChange={e => setData("liens", form.data.liens.map((link, i) => i === index ? { ...link, label: e.target.value } : link))} /><input type="url" placeholder="https://…" value={item.url} onChange={e => setData("liens", form.data.liens.map((link, i) => i === index ? { ...link, url: e.target.value } : link))} /><button type="button" className="icon-button" onClick={() => setData("liens", form.data.liens.filter((_, i) => i !== index))}><Trash2 className="h-4 w-4" /></button></div>)}{form.data.liens.length < features.maxLinks && <Button type="button" variant="outline" onClick={() => setData("liens", [...form.data.liens, { label: "", url: "" }])}><Plus className="h-4 w-4" /> Ajouter un lien</Button>}</div></EditorSection>}{features.requiresHours && <EditorSection title="Horaires" note="Les 7 jours sont attendus pour cette formule"><div className="space-y-3">{weekdays.map((jour, index) => <div key={jour} className="repeater-row"><span className="w-24 text-sm font-medium">{jour}</span><input placeholder="09:00–18:00" value={form.data.horaires[index]?.horaire ?? ""} onChange={e => setData("horaires", form.data.horaires.map((item, i) => i === index ? { ...item, horaire: e.target.value } : item))} /></div>)}</div></EditorSection>}{features.hasCatalog && <EditorSection title="Catalogue" note="Sections et articles de la fiche"><div className="space-y-3">{form.data.sections.map((section, index) => <div key={`${section.titre}-${index}`} className="rounded-xl border border-[#e5e7eb] p-4"><div className="flex gap-2"><input className="flex-1" placeholder="Titre de section" value={section.titre} onChange={e => setData("sections", form.data.sections.map((item, i) => i === index ? { ...item, titre: e.target.value } : item))} /><button type="button" className="icon-button" onClick={() => setData("sections", form.data.sections.filter((_, i) => i !== index))}><Trash2 className="h-4 w-4" /></button></div><div className="mt-3 space-y-2">{section.articles.map((article, articleIndex) => <div key={`${article.nom}-${articleIndex}`} className="grid gap-2 md:grid-cols-[1fr_1fr_120px_auto]"><input placeholder="Nom" value={article.nom} onChange={e => setData("sections", form.data.sections.map((item, i) => i === index ? { ...item, articles: item.articles.map((entry, j) => j === articleIndex ? { ...entry, nom: e.target.value } : entry) } : item))} /><input placeholder="Description" value={article.description} onChange={e => setData("sections", form.data.sections.map((item, i) => i === index ? { ...item, articles: item.articles.map((entry, j) => j === articleIndex ? { ...entry, description: e.target.value } : entry) } : item))} /><input placeholder="Prix" value={article.prix} onChange={e => setData("sections", form.data.sections.map((item, i) => i === index ? { ...item, articles: item.articles.map((entry, j) => j === articleIndex ? { ...entry, prix: e.target.value } : entry) } : item))} /><button type="button" className="icon-button" onClick={() => setData("sections", form.data.sections.map((item, i) => i === index ? { ...item, articles: item.articles.filter((_, j) => j !== articleIndex) } : item))}><Trash2 className="h-4 w-4" /></button></div>)}<Button type="button" variant="outline" onClick={() => setData("sections", form.data.sections.map((item, i) => i === index ? { ...item, articles: [...item.articles, { nom: "", description: "", prix: "" }] } : item))}><Plus className="h-4 w-4" /> Ajouter un article</Button></div></div>)}<Button type="button" variant="outline" onClick={() => setData("sections", [...form.data.sections, { titre: "", articles: [] }])}><Plus className="h-4 w-4" /> Ajouter une section</Button></div></EditorSection>}{features.maxPhotos > 0 && <EditorSection title={`Galerie (${form.data.galerie.length}/${features.maxPhotos})`} note="Photos complémentaires"><div className="space-y-3">{form.data.galerie.map((image, index) => <div key={`${image.url}-${index}`} className="repeater-row"><img src={image.url} alt={image.alt} className="h-14 w-14 rounded-lg object-cover" /><input value={image.alt} onChange={e => setData("galerie", form.data.galerie.map((item, i) => i === index ? { ...item, alt: e.target.value } : item))} /><button type="button" className="icon-button" onClick={() => setData("galerie", form.data.galerie.filter((_, i) => i !== index))}><Trash2 className="h-4 w-4" /></button></div>)}{form.data.galerie.length < features.maxPhotos && <MediaPicker title="Ajouter une photo" value="" required={false} loading={uploading === "gallery"} onPick={file => upload(file, "gallery")} onRemove={() => undefined} /></div></EditorSection>}<EditorSection title="Notes internes"><EditorField label="Notes"><textarea rows={5} value={form.data.notesInternes} onChange={e => setData("notesInternes", e.target.value)} /></EditorField></EditorSection><section className="editor-card"><p className="eyebrow">Contrôle qualité</p><p className="mt-2 text-sm text-[#667085]">{requestsQuery.data?.length ?? 0} demande(s) de contact enregistrée(s).</p></section></div></main><AdminClientInvitationPanel /></div>;
}
function EditorSection({ title, note, children }: { title: string; note?: string; children: React.ReactNode }) { return <section className="editor-card"><div className="mb-5"><h2 className="font-semibold tracking-[-0.02em]">{title}</h2>{note && <p className="mt-1 text-xs text-[#8a93a1]">{note}</p>}</div>{children}</section>; }
function EditorField({ label, children }: { label: string; children: React.ReactNode }) { return <label className="field-label"><span>{label}</span>{children}</label>; }
function MediaPicker({ title, value, required, loading, onPick, onRemove }: { title: string; value: string; required: boolean; loading: boolean; onPick: (file: File) => void; onRemove: () => void; }) { return <div className="rounded-xl border border-[#e5e7eb] bg-[#fafbfc] p-4"><div className="flex items-center justify-between"><div><p className="text-sm font-medium">{title}</p>{required && <p className="text-xs text-amber-600">Requis par la formule</p>}</div>{value && <button type="button" className="icon-button" onClick={onRemove}><Trash2 className="h-4 w-4" /></button>}</div>{value ? <img src={value} alt={title} className="mt-4 h-32 w-full rounded-lg object-cover" /> : <label className="mt-4 flex cursor-pointer flex-col items-center justify-center rounded-lg border border-dashed border-[#d8dde4] py-8 text-center"><Upload className="h-5 w-5" /><span className="mt-2 text-sm">{loading ? "Traitement…" : "Choisir une image"}</span><input type="file" accept="image/jpeg,image/png,image/webp" className="hidden" onChange={event => { const file = event.target.files?.[0]; if (file) onPick(file); event.currentTarget.value = ""; }} /></label>}</div>; }
